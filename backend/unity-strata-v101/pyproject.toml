[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "unity-futures-strategy"
version = "0.1.0"
description = "Framework-neutral, regime-aware crypto futures strategy core"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]

